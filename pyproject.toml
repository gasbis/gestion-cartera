[project]
name = "gestion-cartera"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "gasbis", email = "gasbis@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "reflex[db]>=0.9.10.post2",
]

[project.scripts]
gestion-cartera = "gestion_cartera:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
